name: Deployment pipeline

on:
  push:
    branches:
      - main


jobs:
  pages:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v2
        with:
          node-version: "16"
      - name: install
        run: npm install
        working-directory: './blog-app'
      - name: email
        run: git config --global user.email "niila.siilasjoki@gmail.com"
      - name: username
        run: git config --global user.name "K123AsJ0k1"
      - name: deploy
        run: npm run deploy
        working-directory: './blog-app'