name: Deployment pipeline

on:
  push:
    branches:
      - main


jobs:
  pages:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v3
      - name: deploy
        run: npm run deploy